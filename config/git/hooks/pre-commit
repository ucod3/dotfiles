#!/usr/bin/env bash
# ──────────────────────────────────────────────────────────────────────────────
# TEMPLATE — not executed directly.
# The live hook is installed by: scripts/bin/install-hooks
# It runs both validate --quick AND gitleaks (if available).
#
# To install manually:
#   ./scripts/bin/install-hooks
#
# To bypass a single commit:
#   git commit --no-verify
# ──────────────────────────────────────────────────────────────────────────────
echo "This is a template. Run: ./scripts/bin/install-hooks"
exit 1
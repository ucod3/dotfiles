# Environment Variable Exports
export npm_config_prefix="$HOME/.local"

# PNPM_HOME and its PATH entry live in config/zsh/.zshenv — env vars belong
# there per the zsh startup contract, and three copies had drifted apart.

# CLAUDE.md

The agent contract for this repository is vendor-neutral and lives in
[`AGENTS.md`](./AGENTS.md) — repo layout, the `dot` CLI, hard rules R1–R8,
workflow, and the definition of done. It is imported below, so there is nothing
to duplicate here and nothing to drift.

Do not copy rules from `AGENTS.md` into this file. That regrowth is exactly what
ADR-008 exists to stop.

@AGENTS.md

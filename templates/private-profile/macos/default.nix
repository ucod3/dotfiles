# Personal macOS preferences belong here.
#
# Start with no opinionated defaults. Add standard nix-darwin options as you
# choose them, for example `system.defaults.dock.autohide = true;`.
{ ... }:

{
}
